  
#include <iostream.h>
#include <stdlib.h>

#include "File.h"
File::File(file d, char n)
{
	


ajout::retrait(int & res)
{
	if (taille == 0) // verif file vide
		return 0;
		
int pile<>::exists(bool)
{
	if (file)
		return exists(d);
}


// création tableau et vérif mémoire
   int * temp = new int[taille - 1];

       if (temp == NULL)

            return 0;
            
       // récupération donnée et copie     
       res = donnees[0];
	   --taille;
	   for (int i = 0; i < taille; i++)
	   temp[i] = donnees[i + 1];
	   

// ajout données

       delete [] donnees;
	    donnees = temp;
	    return 1;
}
}